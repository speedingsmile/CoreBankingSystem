name: Go CI

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-latest
    services:
      postgres:
        image: postgres:latest
        env:
          POSTGRES_USER: user
          POSTGRES_PASSWORD: password
          POSTGRES_DB: ledger_db
        ports:
          - 5432:5432
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5

      zookeeper:
        image: confluentinc/cp-zookeeper:7.3.0
        env:
          ZOOKEEPER_CLIENT_PORT: 2181
          ZOOKEEPER_TICK_TIME: 2000
        ports:
          - 2181:2181

      kafka:
        image: confluentinc/cp-kafka:7.3.0
        env:
          KAFKA_BROKER_ID: 1
          KAFKA_ZOOKEEPER_CONNECT: zookeeper:2181
          KAFKA_ADVERTISED_LISTENERS: PLAINTEXT://kafka:29092,PLAINTEXT_HOST://localhost:9092
          KAFKA_LISTENER_SECURITY_PROTOCOL_MAP: PLAINTEXT:PLAINTEXT,PLAINTEXT_HOST:PLAINTEXT
          KAFKA_INTER_BROKER_LISTENER_NAME: PLAINTEXT
          KAFKA_OFFSETS_TOPIC_REPLICATION_FACTOR: 1
        ports:
          - 9092:9092
        options: >-
          --health-cmd "cub kafka-ready -b localhost:9092 1 20"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 10

    steps:
    - uses: actions/checkout@v4

    - name: Set up Go
      uses: actions/setup-go@v4
      with:
        go-version: '1.23'

    - name: Wait for Postgres
      run: |
        while ! pg_isready -h localhost -p 5432 -U user; do
          sleep 1
        done

    - name: Initialize Database
      env:
        PGPASSWORD: password
      run: |
        psql -v ON_ERROR_STOP=1 -h localhost -p 5432 -U user -d ledger_db -f db/schema.sql
        psql -v ON_ERROR_STOP=1 -h localhost -p 5432 -U user -d ledger_db -f update_clients_accounts_schema.sql
        psql -v ON_ERROR_STOP=1 -h localhost -p 5432 -U user -d ledger_db -f update_config_lifecycle_schema.sql
        psql -v ON_ERROR_STOP=1 -h localhost -p 5432 -U user -d ledger_db -f update_product_schema.sql
        psql -v ON_ERROR_STOP=1 -h localhost -p 5432 -U user -d ledger_db -f update_reference_audit_schema.sql
        psql -v ON_ERROR_STOP=1 -h localhost -p 5432 -U user -d ledger_db -f update_schema.sql
        psql -v ON_ERROR_STOP=1 -h localhost -p 5432 -U user -d ledger_db -f update_securities_schema.sql

    - name: Debug Database
      env:
        PGPASSWORD: password
      run: |
        psql -h localhost -p 5432 -U user -d ledger_db -c "\dt"
        psql -h localhost -p 5432 -U user -d ledger_db -c "SELECT * FROM pg_extension;"

    - name: Build
      run: go build -v ./...

    - name: Test
      env:
        DB_HOST: localhost
        DB_PORT: 5432
        DB_USER: user
        DB_PASSWORD: password
        DB_NAME: ledger_db
        KAFKA_BROKER: localhost:9092
        KAFKA_BROKERS: localhost:9092
      run: go test -v ./...
